# Example frontend environment variables (do NOT commit secrets here)
# Public variables (VITE_ prefix) are embedded in the client bundle.
# Keep secrets on the server only.

# Base API URL for the frontend to contact your backend (set in Render for production)
VITE_API_URL=

# Public EmailJS keys (ok to be public)
VITE_EMAILJS_SERVICE_ID=
VITE_EMAILJS_TEMPLATE_ID=
VITE_EMAILJS_PUBLIC_KEY=

# Gemini model name (public)
VITE_GEMINI_MODEL=gemini-2.5-flash

# NOTE: If you have a private Gemini or Google API key, DO NOT add it here.
# Move private keys to the backend and call them through server endpoints.